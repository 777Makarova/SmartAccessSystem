{% extends 'base.html.twig' %}

{% block title %}Token index{% endblock %}

{% block body %}

    <div style="padding-top:150px ">
    <span>
        <h1>Token index</h1>

        <table class="table">
            <thead>
            <tr>
                <th>User_id</th>
                <th>Token</th>
                <th>Id</th>
                <th>DateCreate</th>
                <th>DateUpdate</th>
                <th>actions</th>
            </tr>
            </thead>
            <tbody>
            {% for token in tokens %}
                <tr>
                    <td>{{ token.used_id}}</td>
                    <td>{{ token.token }}</td>
                    <td>{{ token.id }}</td>
                    <td>{{ token.dateCreate ? token.dateCreate|date('Y-m-d H:i:s') : '' }}</td>
                    <td>{{ token.dateUpdate ? token.dateUpdate|date('Y-m-d H:i:s') : '' }}</td>
                    <td>
    {#                    <a href="{{ path('app_user_user_show', {'id': user.id}) }}">show</a>#}
    {#                    <a href="{{ path('app_user_user_edit', {'id': user.id}) }}">edit</a>#}

                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="7">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </span>
    </div>


{#    <a href="{{ path('app_user_user_new') }}">Create new</a>#}
{% endblock %}
